language: objective-c
osx_image: xcode7.3
install:
    - sudo gem install cocoapods
    - pod repo update 2>&1 >/dev/null
    - pod install --verbose
    - xcodebuild -workspace NetProbe.xcworkspace/ -scheme NetProbe
      CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO
